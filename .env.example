# Discord Bot Token
DISCORD_TOKEN=your_discord_bot_token_here

# Optional: Path to config file (defaults to config.json)
CONFIG_PATH=config.json

# Optional: Master user ID (for bot owner commands)
MASTER_USER=your_user_id_here

# Optional: Default prefix
DEFAULT_PREFIX=.

# Optional: Database path
DATABASE_PATH=yuno.db
